"use client";
import { FormEvent, useEffect, useState } from "react";
import { sendEmailVerification } from "firebase/auth";
import { Brand } from "../../../components/brand";
import { firebaseAuth } from "../../../lib/firebase";
const base = (process.env.NEXT_PUBLIC_SIRALOOM_API_BASE ?? "http://localhost:8000/api/v1").replace(/\/$/, "");
export default function Onboarding() { const [name, setName] = useState(""); const [message,setMessage]=useState(""); const [ready,setReady]=useState(false); useEffect(()=>{ const user=firebaseAuth?.currentUser; if(!user) { window.location.assign("/login"); return; } setReady(true); },[]); const submit=async(e:FormEvent)=>{e.preventDefault(); const user=firebaseAuth?.currentUser; if(!user)return; if(!user.emailVerified){setMessage("Verify your email first, then refresh this page."); return;} try { const token=await user.getIdToken(true); const res=await fetch(`${base}/auth/onboarding/organization`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`},body:JSON.stringify({organization_name:name})}); const body=await res.json(); if(!res.ok)throw new Error(body.detail ?? "Onboarding failed"); window.location.assign("/app/dashboard"); } catch(err){setMessage(err instanceof Error?err.message:"Onboarding failed");}}; if(!ready)return <main className="loading">Preparing secure onboarding…</main>; return <main className="auth-page"><Brand /><form className="auth-card" onSubmit={submit}><p className="eyebrow">ORGANIZATION ONBOARDING</p><h1>Set up your laboratory workspace</h1><p>Organization access is created server-side. Your browser cannot choose a role.</p><label>Organization name<input value={name} onChange={e=>setName(e.target.value)} required /></label>{message&&<p className="form-message">{message}</p>}<button className="button">Create organization</button><button className="text-button" type="button" onClick={()=>firebaseAuth?.currentUser&&sendEmailVerification(firebaseAuth.currentUser).then(()=>setMessage("Verification email sent."))}>Resend verification email</button></form></main>; }

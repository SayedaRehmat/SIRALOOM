"use client"; import Link from "next/link"; import { useSearchParams } from "next/navigation"; import { AuthForm } from "../../../components/auth-form"; import { Brand } from "../../../components/brand"; import { LanguageSwitcher } from "../../../components/language-switcher"; import { useLanguage } from "../../../lib/i18n"; export default function Signup(){const params=useSearchParams();const{t}=useLanguage();const isOrganizationSignup=params.get("mode")==="organization";return <main className="auth-page"><div className="auth-language"><LanguageSwitcher /></div><Brand /><AuthForm mode="signup" trial={!isOrganizationSignup}/><p className="text-muted">{isOrganizationSignup?<Link href="/signup">{t("auth.startFreeTrial")} →</Link>:<Link href="/signup?mode=organization">{t("auth.haveAccount")} {t("public.services")} →</Link>}</p></main>}
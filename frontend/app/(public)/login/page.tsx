import { AuthForm } from "../../../components/auth-form"; import { Brand } from "../../../components/brand";
export default function Login() { return <main className="auth-page"><Brand /><AuthForm mode="login" /></main>; }
